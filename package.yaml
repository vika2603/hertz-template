layouts:
  - path: handler.go
    disable: true
  # NOTE: keep in sync with the biz/router/register.go template in layout.yaml.
  - path: register.go
    body: |-
      package router

      import (
        "sort"

        "go.uber.org/fx"
        {{$.DepPkgAlias}} "{{$.DepPkg}}"
        ping "{{trimSuffix (printf "/%s" $.DepPkgAlias) $.DepPkg}}/ping"
      )

      func Options() fx.Option {
          r := make(map[string]fx.Option)

          //INSERT_POINT: DO NOT DELETE THIS LINE!
          {{$.DepPkgAlias}}.Register(r)

          ping.Register(r)

          names := make([]string, 0, len(r))
          for name := range r {
              names = append(names, name)
          }
          sort.Strings(names)

          opts := make([]fx.Option, 0, len(r))
          for _, name := range names {
              opts = append(opts, r[name])
          }
          return fx.Options(opts...)
      }

  - path: "router.go"
    update_behavior:
      type: cover
    body: |-
      // Code generated by hz. DO NOT EDIT.

      package {{$.PackageName}}

      {{- $pkg := $.PackageName}}
      {{- $Pkg := title $pkg}}

      import (
          "go.uber.org/fx"
          "github.com/cloudwego/hertz/pkg/app/server"
          {{- range $k, $v := .HandlerPackages}}
          {{- $handlerPath := trimSuffix (printf "/%s" $k) $v}}
          "{{$handlerPath}}"
          {{- $bizBase := trimSuffix "/handler" $handlerPath}}
          {{$pkg}}service "{{$bizBase}}/service/{{$pkg}}"
          {{- end}}
      )

      {{define "g"}}
      {{- if eq .Path "/"}}r
      {{- else}}{{.GroupName}}{{end}}
      {{- end}}

      {{define "G"}}
      {{- if ne .Handler ""}}
          {{- .GroupName}}.{{.HttpMethod}}("{{.Path}}", append({{.HandlerMiddleware}}Mw(), h.{{trimPrefix (printf "%s." .HandlerPackageAlias) .Handler}})...)
      {{- end}}
      {{- if ne (len .Children) 0}}
      {{.MiddleWare}} := {{template "g" .}}.Group("{{.Path}}", {{.GroupMiddleware}}Mw()...)
      {{- end}}
      {{- range $_, $router := .Children}}
      {{- if ne .Handler ""}}
          {{template "G" $router}}
      {{- else}}
          {   {{template "G" $router}}
          }
      {{- end}}
      {{- end}}
      {{- end}}

      func Register(r map[string]fx.Option) {
          r["{{$pkg}}"] = fx.Module("{{$pkg}}",
              fx.Provide({{$pkg}}service.NewService, handler.New{{$Pkg}}Handler),
              fx.Invoke(RegisterRouter),
          )
      }

      func RegisterRouter(r *server.Hertz, h *handler.{{$Pkg}}Handler) {
      {{- template "G" .Router}}
      }

  - path: "{{.HandlerDir}}/{{.GenPackage}}.go"
    loop_service: true
    update_behavior:
      type: cover
    body: |-
      {{- $pkg := .IDLPackageInfo.GenPackage}}
      {{- $Pkg := title $pkg}}
      {{- $mod := .IDLPackageInfo.GoModule}}
      {{- $modelDir := .IDLPackageInfo.ModelDir}}

      // Code generated by hz. DO NOT EDIT.

      package handler

      import (
          "context"

          "github.com/cloudwego/hertz/pkg/app"

          "{{$mod}}/{{$modelDir}}/{{$pkg}}"
          "{{$mod}}/pkg/handler"
      )

      type {{$Pkg}}Handler struct {
          resp handler.Responder
          svc  {{$pkg}}.{{$Pkg}}Service
      }

      func New{{$Pkg}}Handler(resp handler.Responder, svc {{$pkg}}.{{$Pkg}}Service) *{{$Pkg}}Handler {
          return &{{$Pkg}}Handler{resp: resp, svc: svc}
      }
      {{range .Methods}}
      func (h *{{$Pkg}}Handler) {{.Name}}(ctx context.Context, c *app.RequestContext) {
          var req {{$pkg}}.{{.RequestTypeRawName}}
          if err := c.BindAndValidate(&req); err != nil {
              h.resp.Fail(c, err)
              return
          }

          resp, err := h.svc.{{.Name}}(ctx, &req)
          if err != nil {
              h.resp.Fail(c, err)
              return
          }

          h.resp.Success(c, resp)
      }
      {{end}}

  - path: "{{.ModelDir}}/{{.GenPackage}}/service.go"
    loop_service: true
    update_behavior:
      type: cover
    body: |-
      // Code generated by hz. DO NOT EDIT.

      package {{.FilePackage}}

      import "context"

      type {{title .FilePackage}}Service interface {
          {{- range .Methods}}
          {{.Name}}(ctx context.Context, req *{{.RequestTypeRawName}}) (*{{.ReturnTypeRawName}}, error)
          {{- end}}
      }

  - path: "biz/service/{{.GenPackage}}/service.go"
    loop_service: true
    update_behavior:
      type: skip
    body: |-
      {{- $pkg := .FilePackage}}
      {{- $Pkg := title $pkg}}
      package {{$pkg}}

      import "{{.IDLPackageInfo.GoModule}}/{{.IDLPackageInfo.ModelDir}}/{{$pkg}}"

      type Service struct{}

      func NewService() {{$pkg}}.{{$Pkg}}Service {
          return &Service{}
      }

  - path: "biz/service/{{.GenPackage}}/{{ToSnakeCase .MethodName}}.impl.go"
    loop_method: true
    update_behavior:
      type: skip
    body: |-
      {{- $pkg := .FilePackage}}
      package {{$pkg}}

      import (
          "context"
          "errors"

          "{{.IDLPackageInfo.GoModule}}/{{.IDLPackageInfo.ModelDir}}/{{$pkg}}"
      )

      func (s *Service) {{.Name}}(ctx context.Context, req *{{$pkg}}.{{.RequestTypeRawName}}) (*{{$pkg}}.{{.ReturnTypeRawName}}, error) {
          return nil, errors.New("not implemented")
      }
